caldera_cycleways_incident_streetview_check:
    path:     /streetviewcheck
    defaults: { _controller: AppBundle:Incident:googleMapsCoord }
    options:
        expose: true

caldera_cycleways_incident_source:
    path:     /incidents
    defaults: { _controller: AppBundle:Incident:load }
    options:
        expose: true
